{
  "name": "fantasia-learning-game",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "description": "Plataforma infantil de microjogos educativos orientada por conteúdo.",
  "license": "UNLICENSED",
  "packageManager": "pnpm@11.19.0",
  "engines": {
    "node": ">=22.12 <25",
    "pnpm": ">=11 <12"
  },
  "scripts": {
    "check": "pnpm lint && pnpm format:check && pnpm typecheck && pnpm test && pnpm build && pnpm qa:performance",
    "dev": "pnpm --filter @fantasia/web dev",
    "dev:harness": "pnpm --filter @fantasia/engine-harness dev",
    "dev:prototype": "vite --host 0.0.0.0",
    "build": "pnpm -r --if-present build",
    "build:site": "pnpm build && node scripts/build-site.mjs",
    "e2e": "playwright test",
    "format": "prettier --write apps packages scripts tests package.json pnpm-workspace.yaml tsconfig.base.json eslint.config.js playwright.config.ts",
    "format:check": "prettier --check apps packages scripts tests package.json pnpm-workspace.yaml tsconfig.base.json eslint.config.js playwright.config.ts",
    "lint": "eslint apps packages --max-warnings 0",
    "qa:performance": "node scripts/validate-mobile-budget.mjs",
    "test": "pnpm -r --if-present test",
    "typecheck": "pnpm -r --if-present typecheck"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@playwright/test": "1.62.1",
    "eslint": "10.9.0",
    "eslint-plugin-react-hooks": "7.1.1",
    "eslint-plugin-react-refresh": "0.5.4",
    "globals": "17.11.0",
    "prettier": "3.9.6",
    "typescript": "6.0.3",
    "typescript-eslint": "8.67.0",
    "vite": "8.2.2"
  }
}
